pre-commit:
  parallel: true
  jobs:
    - name: biome lint
      run: "bun run check:lint"

    - name: biome format
      run: "bun run fix:format"
      stage_fixed: true

    - name: biome check
      run: "bun run check"
