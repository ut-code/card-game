pre-commit:
  parallel: true
  jobs:
    - name: biome lint
      run: "bun run check:lint"
      stage_fixed: true

    - name: biome format
      run: "bun run fix:format"
      stage_fixed: true

    - name: biome check
      run: "bun run check"
